orbs:
  maven: circleci/maven@1.1.0
version: 2.1
workflows:
  maven_test:
    jobs:
      - maven/test
          



jobs:
  maven/test:
    docker:
      - image: cimg/openjdk:16.0.0
      
    working_directory: ~/Mo_RESTAPI_Task/project
    environment:
          # Customize the JVM maximum heap limit
          MAVEN_OPTS: -Xmx3200m
          
    steps:
      - checkout
      - run: java --version
      - run: cd repo; mvn dependency:go-offline
      - run: mvn clean test
      - save_cache:
          key: dmp-{{ checksum "pom.xml" }}
          paths:
            - ~/.m2
